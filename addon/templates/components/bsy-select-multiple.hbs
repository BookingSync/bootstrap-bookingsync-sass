{{#if label}}
  <label for="{{inputId}}">{{label}}</label>
{{/if}}
{{#power-select-multiple
  selected=selected
  options=options
  onchange=onchange
  placeholder=placeholder
  id=inputId
  class="form-control"
  as |option name|
}}
  {{yield option name}}
{{/power-select-multiple}}
