<label for="{{id}}">{{label}}</label>
{{#if icon}}
  <i class="icon-{{icon}} icon--{{if size size "default"}}"></i>
{{/if}}
{{one-way-input
  value=value
  update=update
  class=inputClassNames
  type=type
  id=inputId
  readonly=readonly
}}
